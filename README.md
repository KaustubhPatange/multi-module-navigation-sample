# multi-module-test (hilt)

- This approach demonstrates multi-module navigation purely using hilt.
- No dagger multi-map binding stuff.
- Injecting in `ViewModel` & `WorkManager` becomes straightforward.
